requests
beautifulsoup4
pandas
requests-html
lxml_html_clean
playwright
tweepy
nest-asyncio
psycopg2-binary
tiktokapipy

# Core NLP/AI pipeline requirements
sentence-transformers  # For Sentence-BERT embeddings
bertopic  # For topic modeling (includes UMAP, HDBSCAN)
umap-learn  # UMAP for dimensionality reduction (dependency for BERTopic)
hdbscan  # HDBSCAN for clustering (dependency for BERTopic)
openai  # For OpenAI GPT-4 API
langchain  # For LLM workflow integration
nltk  # For text preprocessing (stopwords, tokenization, lemmatization)
scikit-learn  # For ML utilities (used by BERTopic, etc)
numpy  # For numerical operations
